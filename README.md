# بلوك هَب

هذه النسخة لا تحتاج `mods_data.json` ولا تحتاج GitHub Actions لكي يظهر الموقع.

`index.html` يجلب بيانات Modrinth مباشرة من المتصفح باستخدام API العام.

Modrinth يدعم CORS، لذلك يمكن للموقع إجراء طلبات API مباشرة من المتصفح. 
